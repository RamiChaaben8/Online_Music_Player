name: Release

on:
  push:
    tags:
      - 'v*.*.*'
      - 'V*.*.*'
      - 'v*.*'
      - 'V*.*'
  workflow_dispatch:
    inputs:
      release_tag:
        description: Existing release tag to build and attach assets to
        required: true
        default: V1.2
        type: string

permissions:
  contents: write

env:
  UTIFY_RELEASE_TAG: ${{ github.ref_name == 'V1.2-android' && 'V1.2' || inputs.release_tag || github.ref_name }}

jobs:

  # ── Windows build ───────────────────────────────────────────────────────────
  build-windows:
    name: Build Windows
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Flutter version
        run: flutter --version

      - name: Install dependencies
        run: flutter pub get

      - name: Install NuGet
        uses: nuget/setup-nuget@v2

      - name: Enable Windows desktop
        run: flutter config --enable-windows-desktop

      - name: Build Windows release
        run: flutter build windows --release

      - name: Build installer (.exe)
        shell: pwsh
        env:
          RELEASE_TAG: ${{ env.UTIFY_RELEASE_TAG }}
        run: |
          # Extract version from tag (strip leading V or v)
          $version = "$env:RELEASE_TAG" -replace '^[Vv]', ''
          if ($version.Split('.').Length -eq 2) { $version = "$version.0" }
          Write-Host "Building installer for version $version"

          # Inno Setup is pre-installed on GitHub Actions windows-latest runners
          $iscc = "C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
          if (-not (Test-Path $iscc)) {
            # Fallback path for newer runner images
            $iscc = "C:\Program Files\Inno Setup 6\ISCC.exe"
          }
          if (-not (Test-Path $iscc)) {
            Write-Error "Inno Setup not found — installing via Chocolatey"
            choco install innosetup --no-progress -y
            $iscc = "C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
          }

          # Create output directory
          New-Item -ItemType Directory -Force -Path "installer\Output" | Out-Null

          # Compile the installer, injecting the version number
          & $iscc "installer\utify.iss" /DMyAppVersion=$version
          if ($LASTEXITCODE -ne 0) { throw "Inno Setup compilation failed" }

          # Rename output to predictable name
          Move-Item "installer\Output\utify-setup.exe" "utify-windows-setup.exe" -Force
          Write-Host "Installer size: $((Get-Item utify-windows-setup.exe).Length) bytes"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: utify-windows
          path: utify-windows-setup.exe
          retention-days: 1

  # ── macOS build (disabled — uncomment when ready) ──────────────────────────
  # build-macos:
  #   name: Build macOS
  #   runs-on: macos-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: subosito/flutter-action@v2
  #       with:
  #         channel: 'stable'
  #         cache: true
  #     - run: flutter pub get
  #     - run: flutter build macos --release
  #     - name: Zip macOS build
  #       run: |
  #         cd build/macos/Build/Products/Release
  #         zip -r ../../../../../utify-macos.zip Utify.app
  #     - uses: actions/upload-artifact@v4
  #       with:
  #         name: utify-macos
  #         path: utify-macos.zip
  #         retention-days: 1

  # ── Linux build (disabled — uncomment when ready) ──────────────────────────
  # build-linux:
  #   name: Build Linux
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Install build deps
  #       run: |
  #         sudo apt-get update -y
  #         sudo apt-get install -y clang cmake ninja-build pkg-config \
  #           libgtk-3-dev liblzma-dev libstdc++-12-dev
  #     - uses: subosito/flutter-action@v2
  #       with:
  #         channel: 'stable'
  #         cache: true
  #     - run: flutter pub get
  #     - run: flutter build linux --release
  #     - name: Package Linux build
  #       run: tar -czf utify-linux.tar.gz -C build/linux/x64/release/bundle .
  #     - uses: actions/upload-artifact@v4
  #       with:
  #         name: utify-linux
  #         path: utify-linux.tar.gz
  #         retention-days: 1

  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest
    outputs:
      signing_mode: ${{ steps.signing.outputs.mode }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Select Android signing mode
        id: signing
        shell: bash
        env:
          KEYSTORE: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          STORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          if [[ -z "$KEYSTORE" || -z "$STORE_PASSWORD" || -z "$KEY_ALIAS" || -z "$KEY_PASSWORD" ]]; then
            echo "mode=debug" >> "$GITHUB_OUTPUT"
            echo "Android signing secrets are not configured; building a debug-signed APK."
          else
            echo "mode=release" >> "$GITHUB_OUTPUT"
            echo "$KEYSTORE" | base64 --decode > android/app/upload-keystore.jks
            printf 'storePassword=%s\nkeyPassword=%s\nkeyAlias=%s\n' "$STORE_PASSWORD" "$KEY_PASSWORD" "$KEY_ALIAS" > android/app/key.properties
          fi
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
      - name: Build APK
        env:
          SIGNING_MODE: ${{ steps.signing.outputs.mode }}
          RELEASE_TAG: ${{ env.UTIFY_RELEASE_TAG }}
        run: |
          VERSION="$(echo "$RELEASE_TAG" | sed -E 's/^[vV]//')"
          if [[ "$VERSION" =~ ^[0-9]+\.[0-9]+$ ]]; then VERSION="$VERSION.0"; fi
          IFS=. read -r MAJOR MINOR PATCH <<< "$VERSION"
          BUILD_NUMBER=$((MAJOR * 1000000 + MINOR * 1000 + PATCH))
          if [[ "$SIGNING_MODE" == "release" ]]; then
            flutter build apk --release --build-name="$VERSION" --build-number="$BUILD_NUMBER"
            cp build/app/outputs/flutter-apk/app-release.apk utify-android.apk
          else
            flutter build apk --debug --build-name="$VERSION" --build-number="$BUILD_NUMBER"
            cp build/app/outputs/flutter-apk/app-debug.apk utify-android.apk
          fi
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: utify-android
          path: utify-android.apk
          retention-days: 1

  # ── Publish GitHub Release ─────────────────────────────────────────────────
  release:
    name: Publish Release
    runs-on: ubuntu-latest
    needs: [build-windows, build-android]
    # Add build-macos and build-linux to the needs list when those jobs are enabled.

    steps:
      - name: Checkout (for commit history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: utify-windows
          path: artifacts/

      - name: Download Android artifact
        uses: actions/download-artifact@v4
        with:
          name: utify-android
          path: artifacts/

      - name: List artifacts
        run: ls -lh artifacts/

      - name: Generate changelog
        id: changelog
        run: |
          PREV=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")
          if [ -z "$PREV" ]; then
            NOTES=$(git log --pretty=format:"- %s%n%b" | head -30)
          else
            NOTES=$(git log "${PREV}..HEAD" --pretty=format:"- %s%n%b")
          fi
          {
            echo "notes<<EOF"
            echo "$NOTES"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release and upload assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.UTIFY_RELEASE_TAG }}
          name: "Utify ${{ env.UTIFY_RELEASE_TAG }}"
          body: |
            ## What's new in ${{ env.UTIFY_RELEASE_TAG }}

            ${{ steps.changelog.outputs.notes }}

            ---
            **Platform support:** Windows ✅ &nbsp;|&nbsp; Android ${{ needs.build-android.outputs.signing_mode == 'release' && '✅ (release-signed)' || '✅ (debug-signed; may require uninstalling the existing app)' }} &nbsp;|&nbsp; macOS 🔜 &nbsp;|&nbsp; Linux 🔜
          draft: false
          prerelease: false
          overwrite_files: true
          files: artifacts/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
